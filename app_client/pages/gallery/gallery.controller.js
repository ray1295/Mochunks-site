ar app = angular.module('wevativeApp');

app.controller('GalleryController', function($window, $routeParams){
	var vm = this;

	var currentLocation = window.location.href;
	console.log(currentLocation);

});
